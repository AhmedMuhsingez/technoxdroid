---
import BaseHead from '@/components/BaseHead'
import Header from '@/components/Header'
import Footer from '@/components/Footer'
import ProviderTheme from '@/components/ProviderTheme'
import ProviderAnimations from '@/components/ProviderAnimations'
import TwSizeIndicator from '@/components/TwSizeIndicator'
import ScrollCircle from '@/components/ScrollCircle'
const { title, description, image, articleDate } = Astro.props
---

<html lang='en' class='scroll-smooth'>
	<head>
		<BaseHead title={title} description={description} ogImage={image} articleDate={articleDate} />
		<ProviderTheme />
		<ProviderAnimations />
	</head>
	<body class='bg-background-light dark:bg-background-dark text-stone-950 dark:text-white'>
		<main
			class='forceRTL px-5 sm:mx-auto sm:max-w-2xl sm:px-8 lg:px-0 antialiased md:max-w-6xl grid gap-12 overflow-hidden md:overflow-visible'
		>
			<Header />
			<slot />
			<ScrollCircle />
			<Footer />
		</main>
		<TwSizeIndicator />
		<style>
			body {
				margin-left: calc(100vw - 100%); /* prevent layout shift */
				font-family: 'cairo';
			}
		</style>
	</body>
</html>
