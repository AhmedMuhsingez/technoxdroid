<!-- ---
const handleSroll = () => {}
---

<button
	id='a'
	class='light:text-black fixed bottom-0 right-0 border-solid border-2 border-sky-500 m-8 p-1 rounded-full a showBtn'
	>↑</button
>

<script>
	// Find our component DOM on the page using ID.
	const a = document.querySelector('#a')

	// const handleClick = a?.addEventListener('click', () => {
	// 	window.scrollTo({ top: 0, behavior: 'smooth' })
	// })

	var rootElement = document.documentElement //the whole page itself

	function showScroll() {
		// Do something on scroll
		var scrollTotal = rootElement.scrollHeight - rootElement.clientHeight
		if (rootElement.scrollTop / scrollTotal > 0.8) {
			// Show button
			a?.classList.add('showBtn')
		} else {
			// Hide button
			a?.classList.remove('showBtn')
		}
	}

	// document.addEventListener('scroll', handleScroll)

	document.addEventListener('astro:after-swap', () => {
		showScroll()
		const scrollable = document.getElementsByClassName('a')[0]
		scrollable?.addEventListener('click', () => {
			window.scrollTo({ top: 0, behavior: 'smooth' })
		})
	})
</script>

<style>
	.a {
		z-index: 100;
		opacity: 0;
		transform: translateY(100px);
		transition: all 0.5s ease;
	}
	.showBtn {
		opacity: 1;
		transform: translateY(0);
	}
</style> -->
